# Jack's actual data. It stays on thebeast; it is not published anywhere.
# Projects that need it ship a fixture and fall back to that.

# 887 real note titles out of the Obsidian vault, and the screenshot of them
rain/titles.json
rain/screenshot.png

# a real morning brief, with real detail in it
wire/screenshot.png

# real queue rows and 24h of agent journals
v100-char/brain-journal-24h.log
v100-char/worker-journal-24h.log
v100-char/tasks.json
v100-char/per-task.md

__pycache__/
*.pyc
.DS_Store

# PULSE projects. kind: pulse means "built for Jack, from his own data" - they read
# the live vault (journal, loose ends) and their fixtures carry real course numbers
# and assignment dates. Strangers have no use for them and they are not showpieces,
# so they stay on thebeast. Blocking on them was also blocking every showpiece in
# the same push: placard and motel were built 09-08 and never reached Pages. (09-08)
retry/
ends/
_archive/
